name: Cache
on:
  workflow_dispatch:
permissions: write-all
env:
  GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Cache Clean
        run: gh cache delete --all
